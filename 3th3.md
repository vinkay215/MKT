Thought for 2 seconds```typescriptreact project="java-exercises"
...
```

Tôi đã viết code Java cho 5 bài tập và test cases tương ứng sử dụng JUnit:

1. **Tìm số lớn nhất của 3 số**:

1. Class `TimSoLonNhat` với phương thức `timMax(int a, int b, int c)`
2. Test với các trường hợp khác nhau bao gồm số dương, số âm, số bằng nhau



2. **Kiểm tra một số có phải là số nguyên tố không**:

1. Class `KiemTraSoNguyenTo` với phương thức `laSoNguyenTo(int n)`
2. Test với các số nguyên tố và không nguyên tố, bao gồm cả trường hợp đặc biệt



3. **Giải phương trình bậc nhất**:

1. Class `PhuongTrinhBacNhat` với phương thức `giaiPhuongTrinh(double a, double b)`
2. Test với các trường hợp có nghiệm, vô nghiệm và vô số nghiệm



4. **Giải phương trình bậc hai**:

1. Class `PhuongTrinhBacHai` với phương thức `giaiPhuongTrinh(double a, double b, double c)`
2. Test với các trường hợp có hai nghiệm phân biệt, nghiệm kép, vô nghiệm và trường hợp a=0



5. **Nhập ngày và tính ngày tiếp theo**:

1. Class `NgayTiepTheo` với phương thức `tinhNgayTiepTheo(int ngay, int thang, int nam)`
2. Test với các trường hợp ngày thông thường, cuối tháng, cuối năm, năm nhuận và ngày không hợp lệ
